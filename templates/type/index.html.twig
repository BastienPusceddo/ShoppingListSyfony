{% extends 'base.html.twig' %}

{% block title %}Type{% endblock %}

{% block body %}
    <h1>Liste des types</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type in types %}
            <tr>
                <td>{{ type.id }}</td>
                <td>{{ type.nom }}</td>
                <td>
                    <a href="{{ path('admin_type_show', {'id': type.id}) }}">Détail</a>
                    <a href="{{ path('admin_type_edit', {'id': type.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucun type</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_type_new') }}">Nouveau type</a>
    <a href="{{ path('admin_index') }}">Retour</a>
{% endblock %}
