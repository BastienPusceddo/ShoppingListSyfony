{% extends 'base.html.twig' %}

{% block title %}Détail{% endblock %}

{% block body %}
    {{ include('utilitaire/headerAdmin.html.twig') }}
    <h1>Détail type</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_type_index') }}">Retour</a>

    <a href="{{ path('admin_type_edit', {'id': type.id}) }}">Modifier</a>

    {{ include('utilitaire/footerAdmin.html.twig') }}
{% endblock %}
