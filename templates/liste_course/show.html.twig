{% extends 'base.html.twig' %}

{% block title %}ListeCourse{% endblock %}

{% block body %}
    <h1>ListeCourse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ liste_course.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ liste_course.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_home') }}">back to list</a>

    <a href="{{ path('app_liste_course_edit', {'id': liste_course.id}) }}">edit</a>

    {{ include('liste_course/_delete_form.html.twig') }}
{% endblock %}
