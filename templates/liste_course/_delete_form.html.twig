<form method="post" action="{{ path('app_liste_course_delete', {'id': liste_course.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ liste_course.id) }}">
    <button class="btn btn-danger px-5 mb-4">Delete</button>
</form>
