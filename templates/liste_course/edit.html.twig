{% extends 'base.html.twig' %}

{% block title %}Edit ListeCourse{% endblock %}

{% block body %}
    {#    header#}
    {{ include('utilitaire/header.html.twig') }}

<div class=" vh-75 mt-5 d-flex flex-column  justify-content-center align-items-center">
    <div class="d-flex flex-column  justify-content-center align-items-center  rounded w-50 bg-primary text-white">
    <h1>Edit ListeCourse</h1>

    {{ include('liste_course/_form.html.twig', {'button_label': 'Update'}) }}



    {{ include('liste_course/_delete_form.html.twig') }}

        <a href="{{ path('app_home') }}" class="pulse text-info mb-2">back to list</a>

    </div>
</div>

    {#    footer#}
    {{ include('utilitaire/footer.html.twig') }}
{% endblock %}
