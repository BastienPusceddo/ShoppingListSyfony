{% extends 'base.html.twig' %}

{% block title %}Edition{% endblock %}

{% block body %}
    {{ include('utilitaire/headerAdmin.html.twig') }}
<div class="m-5 h-100 d-flex flex-column  justify-content-center align-items-center">
    <div class="d-flex flex-column pt-3 justify-content-center align-items-center  rounded w-25 bg-primary text-white">
        <h1>Éditer l'Article</h1>

        {{ include('article/_form.html.twig', {'button_label': 'Valider'}) }}
        {{ include('article/_delete_form.html.twig') }}
        <a href="{{ path('admin_article_index') }}" class="pulse text-info mb-2">Retour à la liste</a>

    </div>
</div>

    {{ include('utilitaire/footerAdmin.html.twig') }}
{% endblock %}
